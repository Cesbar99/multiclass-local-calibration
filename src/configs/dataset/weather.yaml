num_features : 
num_classes : 5 # 10 classes but fixed no need to specify
random_state : 42
batch_size : # 1024 for calibration and 32 for pre-training
variant: #'awgn', 'motion_blur', 'reduced_contrast'
feature_dim: 2048 # Depnds on model used need to check it out!!!!!
class_freqs: [.1, .1, .1, .1, .1]
shift: False
#size: 224
subsamples: [0.05, 0.1, 0.25, 0.4, 0.5, 0.75, 1.0] # calibration set size. If 1 uses 100% of calibration data.
subsample: 1.0
